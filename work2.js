"use strict"


function pow(x, y) {
    let result = 1; 
        for (let i = 0; i < y; i++) { 
            result *= x;
        }

    return result;
}
    const resultAll = pow(4,3);
    console.log(resultAll)
     




